<!DOCTYPE html>
<html>
<head>
  <title>ポートフォリオ</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>

  <!-- Load jQuery first -->
  <%= javascript_include_tag 'https://code.jquery.com/jquery-3.6.0.min.js' %>

  <!-- Load other scripts after jQuery -->
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

  <!-- Load FullCalendar CSS and JS -->
  <%= stylesheet_link_tag 'https://cdnjs.cloudflare.com/ajax/libs/fullcalendar/3.10.2/fullcalendar.min.css' %>
  <%= javascript_include_tag 'https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.1/moment.min.js' %>
  <%= javascript_include_tag 'https://cdnjs.cloudflare.com/ajax/libs/fullcalendar/3.10.2/fullcalendar.min.js' %>

  <style>
    body, html {
      height: 100%;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      overflow: hidden; /* Disable scrolling */
    }

    #content {
      flex: 1;
      overflow: hidden; /* Disable scrolling within the content area */
      padding-top: 60px; /* Ensure content does not go under the header */
      padding-bottom: 60px; /* Ensure content does not go under the footer */
    }

    header {
      position: fixed;
      top: 0;
      width: 100%;
      background: #343a40;
      color: white;
      text-align: center;
      padding: 10px 0;
      z-index: 1000; /* Ensure header is above other content */
    }

    footer {
      position: fixed;
      bottom: 0;
      width: 100%;
      background: #f8f9fa;
      text-align: center;
      padding: 10px 0;
      z-index: 1000; /* Ensure footer is above other content */
    }
  </style>
</head>

  <body>
    <p class="notice"><%= notice %></p>
    <%= render 'shared/header' %>
    <div id="content">
      <%= yield %>
    </div>
    <%= render 'shared/footer' %>
  </body>
</html>